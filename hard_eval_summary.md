# hard 档评测报告（IVD 语料 · 2026-09-13）

本报告回答一个问题：**在接近真实用户提问的问法下，检索与门控还成立吗？**

## 为什么要单独跑 hard 档

easy 档的 query 直接取文档标题原文，存在两个问题：
1. **天花板效应** —— query 与标题逐字相同，content-only 配置也能 Hit@5 = 1.000，**看不出任何配置差异**；
2. **风格偏差** —— 真实用户不会照着标题念，用标题测出的分数不能代表线上表现。

hard 档用 DeepSeek 把标题改写成口语化提问（换掉大部分用词、保留主题），例如：

| | query |
|---|---|
| easy（原文） | `降钙素原测定试剂注册审查指导原则` |
| hard（改写） | `想申报一个测降钙素原的试剂，技术审评这块有什么要求吗？` |

## 方法

- **语料**：IVD（NMPA 器审中心指导原则 172 篇 / 9004 chunks / section-aware 切片）
- **正例（库内）**：172 条 hard query（每篇文档 1 条），gold = 该文档全部 chunk（中位 54 块）
- **负例（库外）**：21 条，按「语料明确不覆盖」分 4 类构造 —— 通用闲聊/技术、企业运营/商务、具体产品故障排查、临床诊疗
- **复现命令**：

```bash
python scripts/build_eval_set.py --corpus ivd --level doc --n 200 --llm   # 生成 hard 集
python scripts/eval_rag.py  --eval-set rag_data/eval_set_ivd_hard.jsonl --sweep 0,3
python scripts/eval_gate.py --eval-set rag_data/eval_set_ivd_hard.jsonl --sweep 3 --natural
```

## 结果 1 · 检索（文档级，172 条）

| 配置 | Hit@1 | Hit@3 | Hit@5 | Hit@10 | MRR@10 |
|---|---|---|---|---|---|
| content-only（历史对照） | 0.610 | 0.727 | 0.767 | 0.831 | 0.682 |
| **title×3 + content（线上现行）** | **0.657** (+4.7pp) | **0.791** (+6.4pp) | **0.814** (+4.7pp) | **0.866** (+3.5pp) | **0.727** (+4.5pp) |

**解读**：
- easy 档里两者都是 1.000（天花板），**hard 档才第一次把「标题进索引」的增益测出来**：Hit@5 +4.7pp、MRR +4.5pp。
- 增益真实，但**幅度远小于 easy 档暗示的**（IVD easy 档无区分度；早期华佗语料的 +51pp 是天花板前的假象）。
- 文档级 gold 很大（中位 54 块），**Recall@k 不可解读**（上限≈k/|gold|），只看 Hit@k 与 MRR。

## 结果 2 · 门控可分性（正例 172 hard ｜负例 21 库外 ｜top_k=4）

| 信号 | AUC | 正例 med | 正例 min | 负例 med | 负例 max | 间隔 |
|---|---|---|---|---|---|---|
| `ratio = top1/mean(top_k)` | 0.404 | 1.05 | 1.00 | 1.06 | 1.18 | −0.18 |
| `gap = top1−top2` | 0.599 | 1.10 | 0.00 | 0.99 | 3.85 | −3.85 |
| **`top1` 绝对分** | **0.978** | 35.71 | **18.97** | 17.16 | **29.28** | **−10.31** |
| `z = (top1−mean)/std` | 0.515 | 1.49 | 0.68 | 1.49 | 1.73 | −1.05 |

**解读（三条关键事实）**：
1. **只有 top1 绝对分有区分力**（AUC 0.978）；ratio / gap / z 三个归一化信号 AUC 仅 0.40–0.60，接近抛硬币。
   → 根因是 IVD 语料 gold 集大（一答案由多块承载），top4 常全部命中同一文档、分数接近，**ratio 恒等 1.0 自毁**。
2. **但 AUC 0.978 是"假完美"**：正例 min 18.97 **低于** 负例 max 29.28，两区间重叠 10.3 分。
3. **FPR≤10% 选出的阈值 t=24.14 贴在噪声底上** —— 随机 4 词探针的 top1 分布 p50=21.5 / **p90=25.0**，阈值与 p90 几乎重合。
   → 意味着该阈值在区分"查询长度/风格"，而非"库内/库外"。

> 复合规则对照（阈值取各单信号 FPR≤10% 选点）：`AND: abs低 且 ratio低` → 库内保留 96.5% / 库外拦截 81.0%；`OR` → 库内保留 1.7%（不可用）。

## 结果 3 · 阈值决策（ABS_GATE 灵敏度）

用 20 条**人工撰写的章节级口语库内问题**（真实提问风格，且每条 top1 命中的都是对题文档）检验：

| ABS_GATE | 库内保留 | 库外拦截 |
|---|---|---|
| 25.4 | 35% | 76% |
| 20.0 | 90% | 48% |
| 15.0 | 100% | 29% |
| **12.0（线上现行）** | **100%** | **10%** |
| 0.0 | 100% | 0% |

这 20 条的真实 top1 落在 **15.4 ~ 37.6**，与库外（0 ~ 29.3）**完全重叠**。
贪心取 24.14 会误拦其中大多数 —— 这正是不能把检索层当判别器的实证。

## 结论与线上口径

线上门控是 `ABS_GATE=12.0` **且** `RATIO_GATE=1.14`（AND，`butler_fusion.py`）：

| 指标 | 实测 |
|---|---|
| 库内误拦（20 条人工章节级口语问题） | **0/20 = 0%** |
| 库外拦截（21 条库外问题，检索层单独） | **2/21 ≈ 10%** |

**定位**：检索层只做「**极端无支撑熔断**」——拦住明显无检索支撑的提问，**不承担判别器职责**。
库外问题的拦截主要依靠另外两层：LLM 自评（logprob）+ 输出层对齐惩罚。

> ⚠ 对外表述纪律：**不要**宣称「检索层能拦截 90% 库外问题」。
> 检索层贡献 10%（保守设置、库内零误伤）；端到端拦截率需在完整三层链路上另测。

## 面试可引用数字（一句话版）

- 「IVD 语料 172 篇 / 9004 chunks，**hard 档**（LLM 口语化改写）Hit@5 **0.814**，标题加权相对基线 **+4.7pp**」
- 「我们跑了 hard 档才发现：**easy 档的 1.000 是天花板效应**，真实问法下差距只有 4.7pp —— 所以对外只报 hard 档数字」
- 「门控我们做了两级验证：top1 绝对分 AUC 0.978 看着强，但**正负例区间重叠 10 分、阈值贴在噪声底**，所以把它降级为熔断而非判别器 —— 库内误拦 0%，库外拦 10%，剩下的交给 LLM 自评和输出层」
